# LawChecker Online

Онлайн-сервис для проверки текстов, сайтов, изображений (OCR) и PDF на соответствие требованиям языка.

**Версия 2.0** - Рефакторенная архитектура с улучшенной модульностью, безопасностью и производительностью.

## ✨ Возможности

- ✅ Проверка текста (`Текст`)
- ✅ Проверка одной страницы по URL (`Сайт`)
- ✅ Пакетная проверка списка URL (`Пакетная`)
- ✅ Проверка одного слова (`Слово`)
- ✅ OCR и проверка текста с изображения (`Картинка`)
- ✅ MultiScan сайта/списка URL (`Мульти-скан`): HTML + IMG + PDF
- ✅ Deep-check для уточнения спорных слов
- ✅ Экспорт отчетов (обычные и deep)
- ✅ Сервисные метрики на PostgreSQL
- 🆕 Rate limiting для защиты API
- 🆕 Модульная архитектура
- 🆕 Docker поддержка
- 🆕 Автоматизированные тесты

## 🏗️ Архитектура

Проект использует модульную архитектуру для лучшей поддерживаемости:

```
law/
├── app.py                 # Главный файл приложения
├── checker.py            # Движок проверки текстов
├── config/              # Конфигурация
│   ├── __init__.py
│   └── settings.py
├── utils/               # Утилиты
│   ├── __init__.py
│   ├── db.py           # Работа с БД
│   └── helpers.py      # Вспомогательные функции
├── services/           # Бизнес-логика
│   ├── __init__.py
│   ├── checker_service.py
│   └── ocr_service.py
├── routes/            # Flask Blueprints
│   ├── __init__.py
│   └── page_routes.py
├── templates/        # HTML шаблоны
├── static/          # Статические файлы
├── dictionaries/    # Словари
└── tests/          # Тесты
```

## 🛠️ Стек технологий

- **Backend**: Python 3.11+, Flask 3.0
- **Database**: PostgreSQL 15+ с SQLAlchemy 2.0
- **Parsing**: BeautifulSoup4, pypdf
- **NLP**: pymorphy3, pyaspeller
- **Security**: Flask-Limiter (rate limiting)
- **Testing**: pytest, pytest-cov
- **Deployment**: Docker, Gunicorn

## 🚀 Быстрый старт

### Локальный запуск

1. **Клонировать репозиторий**
```bash
git clone <YOUR_REPO_URL>
cd law
```

2. **Создать и активировать виртуальное окружение**
```bash
python -m venv .venv

# Windows
.venv\Scripts\activate

# Linux/Mac
source .venv/bin/activate
```

3. **Установить зависимости**
```bash
pip install -r requirements.txt
```

4. **Создать `.env`** на основе `.env.example`
```bash
cp .env.example .env
# Отредактируйте .env и установите необходимые переменные
```

5. **Запустить приложение**
```bash
python app.py
```

Сервис будет доступен на `http://localhost:5000`.

### 🐳 Docker запуск (рекомендуется)

1. **Запустить с помощью Docker Compose**
```bash
docker-compose up -d
```

2. **Проверить статус**
```bash
docker-compose ps
docker-compose logs -f web
```

3. **Остановить**
```bash
docker-compose down
```

Приложение будет доступно на `http://localhost:5000`.

## 🧪 Тестирование

Запуск тестов:
```bash
# Все тесты
pytest

# С покрытием
pytest --cov=. --cov-report=html

# Только быстрые тесты
pytest -m "not slow"
```

## ☁️ Railway деплой

1. Подключите репозиторий к Railway
2. Добавьте PostgreSQL в проект
3. Проверьте, что в Variables есть `DATABASE_URL`
4. Railway автоматически:
   - Установит зависимости из `requirements.txt`
   - Запустит приложение через Gunicorn
5. После деплоя проверьте:
   - `/api/metrics`
   - `/api/run-history`
   - `/admin/metrics`

## ⚙️ Переменные окружения

Смотрите `.env.example` для полного списка. Основные:

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `DATABASE_URL` | Строка подключения PostgreSQL | - |
| `FLASK_SECRET_KEY` | Секретный ключ Flask | `change-this-secret-key` |
| `FLASK_ENV` | Окружение (development/production) | `development` |
| `OCR_TIMEOUT` | Таймаут OCR запросов (сек) | `30` |
| `MULTISCAN_MAX_URLS_HARD` | Максимум URL для мультискана | `2000` |
| `METRICS_RETENTION_DAYS` | Срок хранения метрик | `60` |

## 📡 API Endpoints

### Проверка текстов
- `POST /api/check` - Проверка текста
- `POST /api/check-url` - Проверка URL
- `POST /api/batch-check` - Пакетная проверка
- `POST /api/deep-check` - Глубокая проверка
- `POST /api/check-word` - Проверка слова

### OCR
- `POST /api/images/token` - Получение токена OCR
- `POST /api/images/ocr` - OCR изображения
- `POST /api/images/check` - OCR и проверка

### Мультискан
- `POST /api/multiscan/run` - Запуск мультисканирования

### Метрики и история
- `GET /api/metrics` - Метрики системы
- `GET /api/run-history` - История проверок
- `GET /api/history` - История (legacy)

### Экспорт
- `POST /api/export/txt` - Экспорт в TXT
- `POST /api/export/json` - Экспорт в JSON
- `POST /api/export/batch-txt` - Экспорт батч-проверки
- `POST /api/export/multiscan-txt` - Экспорт мультискана

📖 Полная документация: `/api-docs`

## 🔐 Безопасность

- ✅ Rate limiting на всех API endpoints (200 запросов/день, 50/час)
- ✅ CORS настроен для безопасного доступа
- ✅ OCR токены хранятся только в сессии пользователя
- ✅ Метрики агрегированные - тексты не сохраняются
- ✅ Валидация всех входных данных
- ✅ Запуск от непривилегированного пользователя в Docker

## 🤝 Разработка

### Запуск в режиме разработки
```bash
export FLASK_ENV=development
export FLASK_DEBUG=1
python app.py
```

### Запуск тестов
```bash
pytest
```

### Проверка типов
```bash
mypy app.py checker.py config/ utils/ services/
```

## 📝 Changelog

### v2.0.0 (2026-02-20)
- ✨ Полный рефакторинг архитектуры (модульная структура)
- ✨ Добавлен rate limiting для API
- ✨ Добавлена Docker поддержка
- ✨ Добавлены автоматизированные тесты
- ✨ Улучшено логирование
- ✨ Добавлены type hints
- 🐛 Исправлены проблемы с производительностью
- 📚 Обновлена документация

### v1.x
- Базовая версия с монолитной архитектурой

## 📄 Лицензия

MIT (см. `LICENSE`).
